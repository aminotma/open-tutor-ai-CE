# Ollama URL for the backend to connect
# The path '/ollama' will be redirected to the specified backend URL
OLLAMA_BASE_URL='http://localhost:11434'

OPENAI_API_BASE_URL=''
OPENAI_API_KEY=''

# AUTOMATIC1111_BASE_URL="http://localhost:7860"

# DO NOT TRACK
SCARF_NO_ANALYTICS=true
DO_NOT_TRACK=true
ANONYMIZED_TELEMETRY=false


# Environment variables
ENABLE_SIGNUP=true
WEBUI_SECRET_KEY=mysecretkey
SUPPRESS_WEBUI_BANNER=true

# Logging level for Open WebUI
# Options: DEBUG, INFO (default), WARNING, ERROR
# Use WARNING or ERROR to reduce verbose logging in production/development
GLOBAL_LOG_LEVEL=WARNING

# Allow all authenticated users (students, teachers) to see and use base Ollama models.
# Without this, Open WebUI only exposes models to users if they have an explicit
# workspace model entry in the DB — meaning new Ollama models are invisible to students.
# Uncomment the following for local demo only; for educational deployments, leave commented to enforce explicit model assignments.
# BYPASS_MODEL_ACCESS_CONTROL=True